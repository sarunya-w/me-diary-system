<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mediary">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scanner.ScannerActivity"></activity>
        <activity android:name=".report.QrCodeSyncActivity" />
        <activity android:name=".report.QrCodeActivity" />
        <activity android:name=".report.PdfViewerActivity" />
        <activity android:name=".report.ReportActivity" />
        <activity android:name=".diary.DrugActivity" />
        <activity android:name=".diary.EventActivity" />
        <activity android:name=".diary.MoodActivity" />
        <activity android:name=".diary.DiaryActivity" />
        <activity android:name=".menu.MainMenuActivity" />
        <activity
            android:name=".authentication.LoginActivity"
            android:label="@string/app_name">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>